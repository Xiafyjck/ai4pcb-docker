"""fsferry:在共享文件系统上摆渡命令。

前提只有两条——两台机器共享一个文件系统,其中一台没有 SSH 入口。满足这两条
就能用,与具体集群无关。

分两层:
- channel  传输层,把一个共享目录变成可靠消息通道(原子写 + 轮询)
- agent / cli  应用层,架在通道上的远程命令执行

分层是有用的:日后若要让真正的 SSH 跑在这条通道上(ProxyCommand + sshd -i),
换的是应用层,通道本身不用重写。
"""

__version__ = "0.1.0"
